export const baseUrl = window.location.origin + window.location.pathname + '#';
export const path = window.location.hash.replace('#', '');

export const GITHUB_API_ROOT_URL = '//api.github.com/users';
